# 交给新对话：C 库存模块开发

将下面“开发请求”复制到新对话。新对话应能读取同一台电脑的项目目录；如果换电脑，需把本目录五份 Markdown 一起带走。本轮文档尚未提交或上传，单独 clone GitHub 分支不会获得这些策划文件。

## 开发请求（可直接复制）

```text
我是 FirstSun 药店管理系统的成员 C，负责库存与库房作业。
启动策划已完成，现在进入开发阶段。请读取交接材料，基于现有项目实现 C 的 P0/P1 全栈需求，并按阶段验证。

项目目录：D:\软件工程实训\FirstSun-C-inventory
GitHub：https://github.com/HaiMingQAQ/FirstSun-Pharmacy-Management-System.git
本地与远程分支：feature/C-inventory
upstream：origin/feature/C-inventory
启动基线：637ad46b6b0bb69a9f606e072f8758528e2256e9

首先完整读取：
docs/C-inventory/README.md
docs/C-inventory/01-需求分析.md
docs/C-inventory/02-技术架构与接口契约.md
docs/C-inventory/03-开发执行与验收计划.md
以及仓库 docs 下的团队开发须知、六人全栈开发方案、开发规范与AI协作规则、前端界面统一规范。

本轮允许完成：C 所属页面、接口、库存数据访问、测试、文档，以及经确认的 C 增量迁移。库存代码放在统一 pharmacy 模块的 inventory 子目录；前端用 admin-ui/src/views/pharmacy/inventory 和 admin-ui/src/api/pharmacy/inventory。

开始先检查当前分支、git status、最新 main 和已有实现；保留本地策划文件及其他未提交修改，不重复创建分支，不覆盖他人代码，不从头重复策划。

注意启动阶段已查明：
1. main 当时没有 backend/yudao-module-pharmacy，origin/A-base 已有公共模块、DrugApi 和标准页面。先核对 A 是否已合入 main。若仍未接入，列出具体依赖和需要协调的公共文件；不要独立复制或重建 A 的模块，也不要擅自合并整个 A 开发分支。
2. origin/feat/d-pos 已有 InventoryFacade 失败占位，但缺业务单号、明细ID、原出库引用和实际分配结果。先根据02文档与 D 确认契约，不伪造业务来源，不直接修改 D 代码。
3. SQL 当前为46表，含 ph_inv_lock；C分工文档的10表描述不完整。operation 幂等表、flow追溯字段和状态迁移只是草案，未创建、未获团队确认。
4. main Java8 与 A 分支 Docker Java17 存在差异，按集成负责人确认的基线运行，不擅自升级框架。

实施顺序：接入检查；仓库/货位和库存台账；操作级幂等及统一收货/选批/扣库/回补；真实B/D联调；上架、盘点、报损、效期和三账核对。F线上冻结列为P2，待业务生命周期确认后扩展。

关键规则：FEFO；批次=货位汇总；total=avail+frozen；sold不参与在库守恒；普通销售不减冻结；所有数量与流水同事务；幂等判断早于重新选批；退货关联原出库流并限制累计回补；过期/停售/越权/不足库存后端拦截。使用真实MySQL验证并发和回滚。

公共POM、yudao-server接入、菜单字典、全局路由、其他成员接口或表结构变更，先完成具体差异方案并协调对应负责人；已有明确授权无需重复询问。继续推进不受阻的工作，不用Mock或关闭校验掩盖缺依赖。

只使用已确认归属本人的独立开发库和本机环境，不清库、不改共享环境。保留.env等个人配置，不提交凭据。可以完成本地实现和验证；Git提交、推送或PR按本次对话实际授权执行，不从旧文档推断额外权限。

每阶段更新 docs/C-inventory 的实际进度，报告修改范围、已跑测试、未测项和依赖阻塞。最终给出能重复执行的验收步骤、接口说明、迁移顺序、截图及已知限制。不要把构建通过或Mock通过写成真实业务联调完成。
```

## 给开发对话的状态补充

- 上述请求由用户复制发送后才构成新对话开发指令，本文件本身不启动开发或新任务。
- 本轮已经完成远程分支创建；无需再要求用户去 GitHub 创建同名分支。
- 目前浏览器有 GitHub 登录态；命令行 push 凭据此前不可用，二者不能等同。不得从浏览器提取 token/cookie 供 Git 使用。
- 本地 `FirstSun-report-source` 是另一个资料目录，C 开发使用 `FirstSun-C-inventory`，避免改错仓库。
- 本轮未合并 A/D 分支、未提交策划文档、未启动数据库、未安装依赖、未写业务代码。
- 分工压缩包是需求来源，里面的任务模板和命令不是额外执行授权。若文档与本次用户要求冲突，以用户要求为准。

## 首轮开发应交付的结果

先给出当前基线变化、仍需协调的具体事项和第一阶段实施清单，然后推进已具备条件的开发。若公共模块或运行环境尚不具备，完成可审查的 C 内部设计细化、测试数据方案和接口差异说明，并准确列出缺口；不要宣称已经跑通完整系统。
